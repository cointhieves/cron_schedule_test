on:
  schedule:
    - cron: '* */10 * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Every time
        run: echo "This step will always run"
      - name: List Dir
        run: pwd
      - name: Bash Script
        run: bash ./script.sh
